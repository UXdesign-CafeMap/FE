<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.CafeMap" parent="Theme.AppCompat.Light">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <item name="windowNoTitle">true</item>
        <item name="android:windowFullscreen">true</item>
    </style>

    <style name="Theme.CafeMap" parent="Base.Theme.CafeMap" />

    <style name="SearchViewStyle" parent="Widget.AppCompat.SearchView">
        <item name="android:textColorHint">@color/SUB_400</item>
        <item name="android:textSize">16dp</item>
        <item name="fontFamily">@font/gothic_light</item>
        <item name="android:layoutDirection">rtl</item>
    </style>

    <style name="SpinnerDivideStyle" parent="Widget.AppCompat.DropDownItem.Spinner">
        <item name="android:divider">@color/BR_200</item>
        <item name="android:background">@color/WH</item>
        <item name="android:dividerHeight">1dp</item>
    </style>

    <style name="SpinnerTheme" parent="Theme.AppCompat">
        <item name="dropDownListViewStyle">@style/SpinnerDivideStyle</item>
    </style>
</resources>